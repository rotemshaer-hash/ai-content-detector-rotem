{
  "name": "ai-content-detector-rotem",
  "private": true,
  "version": "1.1.0",
  "description": "Client-side AI content detector, plus a Netlify Function that fetches videos from social links (server-side, to get around CORS).",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@distube/ytdl-core": "^4.16.12"
  }
}
